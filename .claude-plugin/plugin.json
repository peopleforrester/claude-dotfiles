{
  "// METADATA": "Plugin manifest for Claude Code plugin system",
  "name": "claude-dotfiles",
  "version": "0.7.2",
  "description": "Configuration layer for Claude Code: skills, agents, rules, hooks, and sandboxed permission profiles, all schema-validated in CI",
  "author": "peopleforrester",
  "license": "MIT",
  "compatibility": "Claude Code 2.1+",

  "// COMPONENTS": "Paths relative to plugin root (commands merged into skills in 0.5.0)",
  "components": {
    "skills": "skills/",
    "agents": "agents/",
    "rules": "rules/",
    "contexts": "contexts/",
    "settings": "settings/",
    "mcp": "mcp/"
  },

  "// INSTALLATION": "What gets installed by default",
  "install": {
    "skills": true,
    "agents": true,
    "rules": ["common"],
    "contexts": true,
    "settings": false,
    "mcp": false
  },

  "// OPTIONAL": "Components requiring explicit opt-in",
  "optional": {
    "templates": "templates/",
    "claude-md": "claude-md/",
    "hooks": "hooks/"
  }
}
