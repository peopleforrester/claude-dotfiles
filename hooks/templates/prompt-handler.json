{
  "// TEMPLATE": "Prompt handler type — runs a single-turn model evaluation before the tool proceeds",
  "// USAGE": "The 'prompt' handler is useful for policy gates, session context injection, and dynamic instructions. Matchers are tool names; scope to inputs with a handler-level `if`.",

  "hooks": {
    "PreToolUse": [
      {
        "// PURPOSE": "Inject a security reminder before edits under an auth/ directory",
        "// NOTE": "Duplicate this group with if: Edit(**/security/**) and if: Edit(**/crypto/**) to cover more paths",
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "prompt",
            "if": "Edit(**/auth/**)",
            "prompt": "SECURITY: You are editing a security-sensitive file. Double-check for injection vulnerabilities, credential exposure, and OWASP Top 10 before proceeding."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "// PURPOSE": "Load dynamic project context at session start",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Read PROJECT_STATE.md if it exists, then reconcile against git log before starting work.",
            "once": true
          }
        ]
      }
    ]
  }
}
